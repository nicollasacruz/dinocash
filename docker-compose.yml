version: '3.8'
services:
  dinocash_web_junior:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./volumes/dinocash:/var/www/html:rw
    container_name: dinocash_web_junior
    environment:
      - VIRTUAL_HOST=dinocash.online
      - LETSENCRYPT_HOST=dinocash.online
      - LETSENCRYPT_EMAIL=nicollasacruz@gmail.com
      - VIRTUAL_PORT=8092
    depends_on:
      - php_dino_junior
    networks:
      - rede_dino_junior
      - reverse-proxy

  php_dino_junior:
    build:
      context: ./php
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./volumes/dinocash:/var/www/html:rw
    networks:
      - rede_dino_junior
      - database

networks:
  rede_dino_junior:
    external: true
  reverse-proxy:
    external: true
    name: reverse-proxy
  database:
    external: true
